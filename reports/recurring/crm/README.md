# reports/recurring/crm/

CRM data-hygiene audits, one file per run, named by date: `YYYY-MM-DD.md`.
The `data-hygiene-audit` skill writes them monthly from the contact and
company snapshots in `data/crm/snapshots/`.
