# reports/recurring/community/

Community digests, one file per run, named by date: `YYYY-MM-DD.md`. The
`community-digest` skill writes them weekly from the community thread
snapshots in `data/social/snapshots/`.
