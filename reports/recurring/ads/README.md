# reports/recurring/ads/

Paid campaign reports, one file per run, named by date: `YYYY-MM-DD.md`.
The `ads-performance` skill writes them weekly from the snapshots in
`data/ads/snapshots/`. `YYYY-MM-DD-pacing.md` is the budget view the
`budget-pacing` skill writes from the same snapshots and the finance export.
