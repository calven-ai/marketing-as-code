# reports/recurring/weekly/

The team's weekly report, one file per run, named by date: `YYYY-MM-DD.md`.
The `weekly-report` skill writes it every Friday from the other folders in
`reports/recurring/` and the status entries in `projects/`.
`YYYY-MM-DD-monthly.md` is the month-end edition.
