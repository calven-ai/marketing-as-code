# Event taxonomy

> **Template — unfilled.** Agents: never guess what an event name means or
> which system emits it.

One row per tracked event that matters to marketing.

| Event name (exact) | Means | Emitted by | Key properties |
| --- | --- | --- | --- |
| [`signed_up`] | | [PostHog / GA4 / product] | |
| [`demo_requested`] | | | |
| [`attended_event`] | [what counts as "attended"?] | [e.g. HubSpot list from Zoom] | |
| [add your own] | | | |

## Marketing events (webinars, conferences)

[How real-world events show up in the data: where attendee lists land
(convention: `data/crm/snapshots/YYYY-MM-DD-<source>-<event>-attendees.csv`),
how attendance links to contacts, how "attended then signed up" is computed.]
