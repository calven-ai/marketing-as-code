# reports/recurring/analytics/

Web and product analytics reports, one file per run, named by date:
`YYYY-MM-DD.md`, built from snapshots in `data/analytics/snapshots/`. No
shipped skill writes here yet; a report follows the same shape as the SEO
ones, from `../../_templates/report.md`.
