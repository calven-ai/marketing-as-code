# Credentials: never commit these
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
id_rsa*
id_ed25519*
credentials*.json
service-account*.json

# OS noise
.DS_Store
Thumbs.db

# Editors and per-machine agent settings
.vscode/
.idea/
.claude/settings.local.json

# Dependencies and caches (playground prototypes may pull these in)
node_modules/
__pycache__/

# Left on the Actions runner by transcripts-process.yml; never committed
.transcripts-run/
