# reports/recurring/pr/

Coverage reports, one file per run, named by date: `YYYY-MM-DD.md`. The
`coverage-tracker` skill writes them monthly from the `*-coverage.csv`
snapshots in `data/pr/snapshots/`.
