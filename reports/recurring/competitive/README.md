# reports/recurring/competitive/

Competitor watch reports, one file per run, named by date: `YYYY-MM-DD.md`.
The `competitor-watch` skill writes them monthly from the
`*-web-competitor-changes.csv` snapshots in `data/accounts/snapshots/`,
against the battlecards in `strategy/competitive/`.
