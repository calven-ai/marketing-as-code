# playgrounds/

Come to meetings with prototypes, not decks.

Each prototype is a disposable subfolder built by an agent from one sentence:
a landing page mock, a campaign concept page, an email sequence preview, an ad
variant board, a storyboard. Explicitly throwaway, never production. After the
meeting, log the decision in `memory/` and archive the prototype.
