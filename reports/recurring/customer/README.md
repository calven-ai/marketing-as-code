# reports/recurring/customer/

Churn-signal reports, one file per run, named by date: `YYYY-MM-DD.md`. The
`churn-signals` skill writes them weekly from the `*-customers.csv`
snapshots in `data/crm/snapshots/` and the product usage snapshots in
`data/analytics/snapshots/`.
