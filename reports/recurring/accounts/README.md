# reports/recurring/accounts/

Target-account signal reports, one file per run, named by date:
`YYYY-MM-DD.md`. The `account-signals` skill writes them weekly from
`data/accounts/target-accounts.csv` and the snapshots in
`data/accounts/snapshots/`.
