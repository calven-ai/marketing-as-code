---
source: repo
last_reviewed:
owner:
---

# Visual identity

> **Template: unfilled.** Type over the brackets, or say `/setup` and answer a
> few questions. In the frontmatter, `last_reviewed` is a date (YYYY-MM-DD)
> and `source` stays `repo` unless you connect a context layer. Keep this file
> and [tokens.json](tokens.json) in agreement: update both in the same commit.

## Colors

| Role | Hex | Usage |
| --- | --- | --- |
| Primary | `#______` | |
| Secondary | `#______` | |
| Background | `#______` | |
| Text | `#______` | |
| Accent / success / warning | `#______` | |

## Typography

- Headings: [family, weights, where it's licensed/loaded from]
- Body: [family]
- Fallback stack: [system stack]

## Logo usage

- Files live in `logos/` (SVG preferred). [Clear-space, minimum size,
  what never happens to the logo.]

## Imagery

[Photography vs illustration, style rules, what on-brand images look like.
This steers the prototype builder and any generated visuals.]

## Templates

`templates/` holds reusable design files: an OG-image background, social
frames. A script that composes article titles over that background in the
colors and fonts above is a job for your coding agent, once the background
exists.
