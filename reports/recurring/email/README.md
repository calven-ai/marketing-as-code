# reports/recurring/email/

Email and marketing-automation reports, one file per run, named by date:
`YYYY-MM-DD.md`. The `email-performance` skill writes them monthly from the
snapshots in `data/email/snapshots/`.
