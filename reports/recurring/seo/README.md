# reports/recurring/seo/

Keyword and ranking deltas, one file per run, named by date:
`YYYY-MM-DD.md`. The `seo-analyst` skill writes them from the two most
recent snapshots in `data/seo/snapshots/`.
